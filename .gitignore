node_modules

bazel-*

.idea/
.vscode/
.history/

bazel-*

**/.DS_Store

**/*.orig

go.sum

.df-credentials.json
secret.json
bigquery.json

yarn-error.log

tmp/

# Credentials & Keys
keys/
test_credentials/bigquery.json

# Local-only agent context (internal infra IDs, private notes) — never committed
CLAUDE.local.md
GEMINI.local.md
AGENTS.local.md

# Superpowers planning artifacts (brainstorm specs + plans) — kept in sqlanvil-private
docs/superpowers/

# Locally-built npm package tarballs (see docs npm_publishing.md)
*.tgz

# Local Supabase CLI stack state (run-supabase-stack.sh)
tools/supabase/.supabase-stack/
