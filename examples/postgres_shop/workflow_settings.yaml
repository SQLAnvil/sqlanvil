warehouse: postgres
defaultDataset: sqlanvil_example
defaultAssertionDataset: sqlanvil_example_assertions
# Real projects pin the sqlanvil core version with:
#   sqlanvilCoreVersion: 1.0.0
# (omitted here so this in-repo example compiles against the local @sqlanvil/core)
vars:
  currency: USD
