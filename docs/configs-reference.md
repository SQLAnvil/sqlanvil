# Configs Reference

TODO: write the sqlanvil configs reference once the new proto schemas
(`WarehouseConfig`, `PostgresOptions`, `SupabaseOptions`) are merged.

For the upstream Dataform configs reference (which still describes the
BigQuery-only behavior sqlanvil inherits today), see:
https://dataform-co.github.io/dataform/docs/reference/configs
