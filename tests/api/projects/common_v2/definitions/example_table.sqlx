config { type: "table" }
select * from ${ref("sa_integration_test", "sample_data")}

-- here ${"is"} a `comment

/* ${"another"} ` backtick ` containing ```comment */

post_operations {
    GRANT SELECT ON ${self()} TO GROUP "${constants.allUsersEmailAddress}"
    ---
    GRANT SELECT ON ${self()} TO GROUP "otherusers@example.com"
}
