config {
  type: "view"
}

select ${when(sqlanvil.projectConfig.vars.fooVar === "bar", "1", "2")} as val union all
select 2 as val union all
select ${when(sqlanvil.projectConfig.warehouse === "mysql", "3", "2")} as val
