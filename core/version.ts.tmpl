// The published @sqlanvil/* package version (sqlanvil's own SemVer line).
export const version = "$SQLANVIL_VERSION";

// The upstream dataform-co/dataform release this build is synced to.
export const dataformVersion = "$DF_VERSION";
